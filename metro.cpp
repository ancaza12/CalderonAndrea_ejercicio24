#include <iostream>
#include <random>
#include <stdlib.h>
#include <cmath>

int main(int sigma=1, int N=100000, float delta=1.0){
	srand48(1)
	for (i=0; i<N;i++){
		float prop = srand48(1) + (drand48()-0.5)*delta
		r = min(1, exp(-prop**2/(2.0*sigma**2))/np.sqrt(2.0*M_PI*sigma**2)/exp(-srand48(1)**2/(2.0*sigma**2))/np.sqrt(2.0*M_PI*sigma**2)
	
	float alpha = drand48()
        if(alpha<r){
			lista.append(propuesta)
		}
            
        else{
			lista.append(srand48(1))
		}
    return np.array(lista)				
				
	}
	
	return 0;
	
}
